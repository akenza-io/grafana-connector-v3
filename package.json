{
    "name": "akenza-grafana-connector",
    "version": "1.0.0",
    "description": "enables the visualisation of akenza device data in Grafana dashboards",
    "scripts": {
        "build": "grafana-toolkit plugin:build",
        "test": "grafana-toolkit plugin:test",
        "dev": "grafana-toolkit plugin:dev",
        "watch": "grafana-toolkit plugin:dev --watch",
        "deploy": "yarn build && rm -rf ./tmp && mkdir -p ./tmp/akenza-grafana-connector && cp -R ./dist/ ./tmp/akenza-grafana-connector/ && cd tmp && zip ./akenza-grafana-connector.zip -r ./akenza-grafana-connector/ && cd .. && rm -Rf ./tmp/akenza-grafana-connector"
    },
    "author": "David Giger",
    "license": "Apache-2.0",
    "dependencies": {
        "@grafana/data": "^9.3.6",
        "@grafana/runtime": "^9.3.6",
        "@grafana/toolkit": "^9.3.6",
        "@grafana/ui": "^9.3.6",
        "@types/lodash": "^4.14.191",
        "build-url": "^6.0.1",
        "rxjs": "^7.5.5",
        "rxjs-compat": "^6.6.7"
    },
    "engines": {
        "node": ">=16 <18"
    }
}
